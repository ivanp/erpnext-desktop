# .NET build output
bin/
obj/
*.user
*.userosscache
*.sln.docstates

# IDE
.vs/
.idea/
.vscode/

# Core / App runtime data during local test
.serpy/
appliance/
data/

# Publish output
publish/
dist/
__ce-goal-prompt*
.worktrees/

# Local dev-only signing material (self-signed test certs, private keys) — never committed
.local-signing/
*.pfx

# WiX Toolset local extension cache (per-repo, from `wix extension add`)
.wix/
